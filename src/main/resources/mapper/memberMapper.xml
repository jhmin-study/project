<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.project.mapper.MemberMapper">
    <select id="selectByEmail" resultType="memberVO">
        select id, member_email, member_password, member_name, status, create_date, update_date
        from tbl_member
        where member_email = #{memberEmail};
    </select>

    <select id="selectByEmailAndMemberPassword" resultType="memberVO">
        select id, member_email, member_password, member_name, status, create_date, update_date
        from tbl_member
        where member_email = #{memberEmail} AND member_password = #{memberPassword}
    </select>

    <insert id="insert">
        insert into tbl_member (id, member_password, member_name)
        values (#{id}, #{memberPassword}, #{memberName})
    </insert>

</mapper>