<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.project.mapper.MemberMapper">
    <select id="selectByMemberEmail" resultType="memberVO">
        select id, member_email, member_password, member_name, status, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail}
    </select>

    <insert id="insert">
        insert into tbl_member (member_email, member_password, member_name)
        values (#{memberEmail}, #{memberPassword}, #{memberName})
    </insert>

    <select id="selectByMemberEmailAndMemberPassword" resultType="memberVO">
        select id, member_email, member_password, member_name, status, created_date, updated_date
        from tbl_member
        where member_email = #{memberEmail} and member_password = #{memberPassword}
    </select>
</mapper>